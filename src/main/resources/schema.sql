CREATE TABLE IF NOT EXISTS users (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY NOT NULL,
  name VARCHAR(255) NOT NULL,
  email VARCHAR(255) NOT NULL,
  CONSTRAINT pk_user PRIMARY KEY (id),
  CONSTRAINT unique_user_email UNIQUE (email)
);

CREATE TABLE IF NOT EXISTS item (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY NOT NULL,
  name VARCHAR(255) NOT NULL,
  description VARCHAR(255) NOT NULL,
  available BOOLEAN NOT NULL,
  owner_id BIGINT NOT NULL,
  CONSTRAINT pk_item PRIMARY KEY (id)
);

CREATE TABLE IF NOT EXISTS booking (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY NOT NULL,
  item_id BIGINT NOT NULL,
  booker_id BIGINT NOT NULL,
  start_date TIMESTAMP NOT NULL,
  end_date TIMESTAMP NOT NULL,
  status VARCHAR(20) NOT NULL,
  CONSTRAINT pk_booking PRIMARY KEY (id)
);

CREATE TABLE IF NOT EXISTS comment (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY NOT NULL,
  item_id BIGINT NOT NULL,
  user_id BIGINT NOT NULL,
  text VARCHAR(1024) NOT NULL,
  created TIMESTAMP NOT NULL,
  CONSTRAINT pk_comment PRIMARY KEY (id)
);

ALTER TABLE item ADD CONSTRAINT IF NOT EXISTS fk_item_owner_id FOREIGN KEY(owner_id) REFERENCES users (id);
ALTER TABLE booking ADD CONSTRAINT IF NOT EXISTS fk_booking_booker_id FOREIGN KEY(booker_id) REFERENCES users (id);
ALTER TABLE booking ADD CONSTRAINT IF NOT EXISTS fk_booking_item_id FOREIGN KEY(item_id) REFERENCES item (id);
ALTER TABLE comment ADD CONSTRAINT IF NOT EXISTS fk_comment_user_id FOREIGN KEY(user_id) REFERENCES users (id);
ALTER TABLE comment ADD CONSTRAINT IF NOT EXISTS fk_comment_item_id FOREIGN KEY(item_id) REFERENCES item (id);
